# Tablero — App Front UberMotor

## Por Hacer
- [ ] **Separación de capas en el backend (`conductores.py`)**: coordinar con el equipo de back — 4 endpoints (documentos, perfil, disponibilidad, ubicación) tienen lógica de negocio directa en el router en vez de un `conductor_service.py`. Subir evidencia del refactor cuando esté listo (ver TABLERO.md del repo back).

## En Progreso
- (vacío)

## Hecho
- (vacío)
